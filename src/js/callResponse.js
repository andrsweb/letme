document.addEventListener('DOMContentLoaded', () => {
    'use strict'

    // callResponse()
})

const callResponse = () => {
    const formModalWrapper = document.querySelector('.form__modal_wrapper')

    formModalWrapper.classList.add('showed')

    // formModalWrapper.classList.add('closed')  For smooth closing.
    // setTimeout(() => popupWrapper.classList.remove('showed'), 350)
    // setTimeout(() => popupWrapper.classList.remove('closed'), 350)
}